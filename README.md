1.Take user’s full name as input
2.Count the number of spaces in full name and store it.
3.Take  Email ID from the user.
4.Read the Mobile Number from the user.
5.Take the age from the user and assign it as an integer
6.Check the following conditions:
•	Full name should have at least one space (minimum two words).
•	Full name does not start or end with a space.
•	Email must  contain ‘@’ and ‘.’ symbols.
•	Email should  not start with ‘@’.
•	Mobile number length should be exactly 10 digits.
•	Mobile number should have only digits
•	Mobile number should not start with 0.
•	Age should be between 18 and 60 (inclusive).
7.If all conditions are satisfied, display
“User Profile is VALID” or else “User Profile is INVALID
